// strings
const learnerName: string = 'Bill';

const lasName: string = 'Gates';

// number
let age: number = 28;

// booleans
let authenticated: boolean = true;

// arrays
const favNumbers: number[] = [1,2,3];
const languages: string[] = ['JS', 'TS', 'C#', 'Python']; 

favNumbers.push(11);
favNumbers.push(33);

languages.push('R');
languages.push('C++');

const company: {name: string, value: number} = {name: 'Microsoft', value: 30000000}